const separator = document.createElement('hr') // Létrehoz egy új hr elemet
document.body.appendChild(separator) // Hozzáadja a body-hoz
const tableDiv = new Area('table') // Létrehoz egy új Area divet a 'table' className-nel
const formDiv = new Area('form') // Létrehoz egy új Area divet a 'form' className-nel